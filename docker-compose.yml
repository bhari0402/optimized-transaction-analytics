version: '3.9'

services:

  postgres:
    image: postgres:16

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: pass@12345six
      POSTGRES_DB: paymentDB

    ports:
      - "5432:5432"

  api:
    build: .

    ports:
      - "8000:8000"

    depends_on:
      - postgres